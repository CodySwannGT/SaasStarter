/**
 * Main ESLint configuration entry point
 * This allows users to simply import '@saas-starter/eslint-config'
 * and it will default to the base configuration
 */
module.exports = require('./base');